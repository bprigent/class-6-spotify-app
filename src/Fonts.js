import './Fonts.css';

export function FontH1(props) {
    return (
        <h1 className="font-H1">{props.copy}</h1>
    );
};

export function FontH2(props) {
    return (
        <h2 className="font-H2">{props.copy}</h2>
    );
};